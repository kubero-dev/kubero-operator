apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubero-operator.fullname" . }}
  labels:
    {{- include "kubero-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubero-operator.selectorLabels" . | nindent 4 }}
