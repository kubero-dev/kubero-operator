apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubero-operator.fullname" . }}-test-connection"
  labels:
    {{- include "kubero-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubero-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
